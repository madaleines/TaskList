<h2 class="my-class"><%= page_title %></h2>

<%= form_for @task do |f| %>

<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :description %>
<%= f.text_area :description %>

<%= f.label :due_date %>
<%= f.date_field :due_date, min: Time.now %>

<%= f.submit %>

<% end %>
